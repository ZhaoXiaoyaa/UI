Discuz
网址：http://127.0.0.1/forum.php
账号：admin   1393269559   （管理员）
      sasa    1393269559   （普通账号）

实战一（继承BaseTestCase）
1.用户登录  (返回值)
    方法：登录（两个参数  有返回值）
    BaseTestCase 调用 browser_engine       选择浏览器  进入网址（调用get）
    定位所涉及元素（用户名框 密码框 登录按钮 登录成功后的用户名）
    文本框输入内容    点击登录按钮（调用方法）
    登录返回值   return self.find_element(*self.name).text
    name=mainPage.login("admin","1393269559")
    if  "admin" in  name
2.默认版块发帖
    方法：点击默认版块  发帖
    定位所涉及元素（默认版块  帖子标题  帖子内容  发表帖子按钮 ）
    点击默认版块    标题框内容框输入内容   点击发表帖子
3.默认版块回帖
    方法：回帖
    定位所涉及元素（回帖框  发表回复按钮）
    回帖框输入内容   点击发表回复
4.用户退出
    方法：退出登录
    定位所涉及元素（退出）
    点击退出
实战二
1.管理员用户登录
    定位所涉及元素（用户名框 密码框 登录按钮 登录成功后的用户名）
    文本框输入内容    点击登录按钮（调用方法）
    登录返回值   return self.find_element(*self.name).text
    name=mainPage.login("admin","1393269559")
    if  "admin" in  name
2.进入默认版块，删除帖子
    方法：删除帖子
    定位所涉及元素(默认模块 帖子标题  删除主题  确认按钮)
    依次点击   默认模块 帖子标题  删除主题  确认按钮
3.进入版块管理，（管理中心--论坛）
    定位所涉及元素（管理中心  密码框  提交按钮  论坛）
    点击管理中心
    激活新页面
    输入密码  点击提交按钮  论坛

4.创建新的版块
    方法：创建新的版块
    进入iframe
    定位所涉及元素（添加新版块  提交按钮）
    依次点击：添加新版块 提交按钮

5.管理员退出登录
    定位所涉及元素（退出管理中心   退出登录）
    依次点击  退出管理中心   退出登录
6.普通用户登录
    定位所涉及元素（用户名框 密码框 登录按钮 登录成功后的用户名）
    文本框输入内容    点击登录按钮（调用方法）
    登录返回值   return self.find_element(*self.name).text
    name=mainPage.login("sasa","1393269559")
    if  "sasa" in  name
7.在新的版块下发帖
    定位所涉及元素(新版块  帖子标题  帖子内容  发表帖子按钮)
    点击新版块   输入标题 内容  点击发表帖子按钮

8.回复帖子
    定位所涉及元素（回帖框  发表回复按钮）
    输入内容  点击发表回复按钮
实战三
1.用户登录
    打开网址
    定位所涉及元素（用户名框 密码框 登录按钮 登录成功后的用户名）
    文本框输入内容    点击登录按钮（调用方法）
    登录返回值   return self.find_element(*self.name).text
    name=mainPage.login("sasa","1393269559")
    if  "sasa" in  name
2.进行帖子搜索
3.搜索haotest帖子
    定位所涉及元素（搜索框  搜索按钮）
    输入haotest  点击搜索按钮  或者  摁回车

4.进入搜索的帖子
    激活新窗口
    定位所涉及元素（搜索到的内容  帖子标题 ）
    依次点击  搜索到的内容  帖子标题

5.验证帖子标题和期望一致
      self.assertEqual(result,"haotest" , msg=result)

6.用户退出

实战四
1.发表投票帖子
    定位所涉及元素（发帖  发起投票  标题框  三个选项框  发起投票按钮）
    点击  发帖  发起投票     输入  标题  选项     点击发起投票按钮

2.进行投票
    定位所涉及元素（单选按钮  提交按钮）
    依次选择  单选按钮  提交按钮

3.取出投票各个选项的名称及投票比例
    定位所涉及元素（各个选项   投票比例）
    输出
4.取出投票的主题名称
    定位所涉及元素（标题）
    输出
